# Backend Server (Oracle VNC)
BACKEND_URL=http://localhost:3001

# Contract Address (Update after deployment)
CONTRACT_ADDRESS=0x...

# Network (development = testnet, production = mainnet)
NODE_ENV=development
